import { ShuffleArrayPipe } from './shuffle-array.pipe';

describe('ShuffleArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ShuffleArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
